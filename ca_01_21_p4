void setup()
{
  size (400,300);
  background(0,0,255);
}

void draw()
{}

void mousePressed()
{
  stroke(0);
  fill(255,0,0);
  ellipseMode(CENTER);
  ellipse(mouseX, mouseY, 40, 40);
}

void keyPressed()
{
  background(0,255,0);
}
